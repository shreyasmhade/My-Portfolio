# suco_partner

This workspace contains a SUCO partner registration landing page built with React and Tailwind CSS.

Quick start (Windows PowerShell):

```powershell
npm install
npm start
```

The landing page component is in `src/components/PartnerLanding.jsx` and is rendered by `src/App.js`.
